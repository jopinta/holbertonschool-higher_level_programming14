def add_integer(a, b=98):

    for i in range(a, b)
    if i =! (a, b)
    raise
    TypeError a must be an integer or b must be an integer

    return (a + b)

if __name__ == "__main__":
    import doctest
    doctest.testmod()
